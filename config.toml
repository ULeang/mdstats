# 设置卡组列的快捷选项
# 注意，由于存储使用的csv的限制，不能含有字符'|'，否则会影响csv的数据
custom_deck_list = ["自奏", "巳剑", "雷火", "码丽丝", "小黄毛"]

# 设置备注列的快捷选项
# 注意，由于存储使用的csv的限制，不能含有字符'|'，否则会影响csv的数据
custom_note_list = [
    "先攻全手坑",
    "后攻全通召",
    "对手小糖人",
    "对手纯也够",
    "信息差",
    "飞龙在天",
    "打完丢g",
    "绝不认输,oi!",
    "超时烙印",
    "无敌小黄毛打完丢g,怎么你不服气?",
]

# 设置统计数据表格的背景色和前景色，rgb格式
stats_tbl_background_color = "#00ff00"
stats_tbl_foreground_color = "#ffffff"

# 设置统计数据表格的单元格宽度(第1列,第2列,第3列)
stats_tbl_column_width = [150, 50, 50]

# 设置对战记录表格的初始单元格宽度(第1列,第2列,第3列,第4列,第5列,第6列)
# 如果不为0，可以通过拖动表头来动态调整宽度
# 如果为0，则宽度跟随内容调整，不可手动调整
record_tbl_column_width = [0, 0, 0, 0, 150, 0]

# 设置程序窗口打开时出现的位置以及大小(x轴，y轴，宽度，高度)
prog_window_init_x_y_width_height = [800, 400, 900, 400]

# 设置是否按日期保存记录数据，false为否，true为是
use_daily_record_csv = false

# 设置匹配的间隔时间，即每多少秒进行一次截图匹配，单位毫秒，注意，调低这个值会增加cpu占用进而导致系统变卡
matcher_sleep_ms = 500

# 设置是否隐藏控制台窗口
hide_console = true